// Funktion zum Aktivieren der Keyboard Tasten, um die Checkboxes und den Button zu aktivieren
// Mit If Abfrage, werden die Booleans true oder false gesetzt, wenn entsprechende Tasten betätigt werden
/*void keyPressed() {
  if (key == 'A' || key == 'a') {
    selectedA = true;
    selectedB = false;
    selectedC = false;
    selectedButton = false;
  } else if (key == 'B' || key == 'b') {
    selectedA = false;
    selectedB = true;
    selectedC = false;
    selectedButton = false;
  } else if (key == 'C' || key == 'c') {
    selectedA = false;
    selectedB = false;
    selectedC = true;
    selectedButton = false;
  } else if (key == ENTER) {
    selectedButton = true;
  }
}*/
